name: Build Mod
on: [push, pull_request]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'
      - name: Setup Gradle and Build
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: 8.10 # GitHub сам установит стабильный Gradle 8.10
      - name: Compile Jar
        run: gradle build --no-daemon # Собираем напрямую через установленный Gradle
      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: MidpointOptimizer-JAR
          path: build/libs/*.jar
          
